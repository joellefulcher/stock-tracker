let stock = null;

$().ready(() => {

    get(1);

    $("#save").click(() => {
    });

});

const update = () => {
    // need to calculate price paid by number of shares
    
    
    stock.push(stocks).update;
}

const remove = () => {
    
   for(var i = 0; i < stocks.length; i++);
     stocks.splice(i);

     $("#delete").click(() => {
    });
    
}

const add = () => {
    stock = new stock;
    stocks.add(stock);
    

    $("#add").click(() => {
    });

}

